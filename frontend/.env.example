# Optional. Leave empty when frontend is served by the Express backend.
# Use this only if deploying frontend and backend as separate Render services.
VITE_API_URL=https://your-backend-service.onrender.com/api
